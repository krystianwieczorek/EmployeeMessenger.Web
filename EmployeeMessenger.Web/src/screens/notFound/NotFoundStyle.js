import { makeStyles } from "@material-ui/core/styles";

export const NotFoundStyle = makeStyles({
  link: {
    textDecoration: "none",
  },
  grid: {
    height: "100%",
  },
  button: {
    marginTop: "10px",
  },
  zoom: {
    zoom: 1.5,
  },
});
